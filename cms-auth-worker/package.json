{
  "name": "hostmate-cms-auth-worker",
  "version": "1.0.0",
  "private": true,
  "description": "Cloudflare Worker implementing the GitHub OAuth handshake for the Decap CMS admin panel at /admin/. Deployed separately from the Next.js site — see README.md.",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250101.0",
    "typescript": "^5",
    "wrangler": "^3.90.0"
  }
}
